# LIBRARY

A PERSISTENT-STRUCT (and so, transitively, a PERSISTENT-OBJECT, GIT-TREE, and STANDARD-OBJECT) generated by DEFINE-PERSISTENT-STRUCT, with one slot per name in (CATALOG). See MAKE-LIBRARY and LIBRARY-P.

## Slots

  * **catalog**: The CATALOG slot of a LIBRARY instance.
